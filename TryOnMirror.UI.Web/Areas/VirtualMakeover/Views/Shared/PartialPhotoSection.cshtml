@model string[]

@*<div style="border-top: 2px solid #333; padding-top: 10px; text-align: center;">
    <a href="#" id="uploadPhoto" class="btn">Use your Photo</a>

    <div class="busy">
        <img src="@Url.Content("~/assets/images/preloader-w8-line-black.gif")" alt="loading..." width="110" height="10"/>
    </div>
    <input id="ImageFile" data-name="userPhoto" data-url="@Url.Action("new-trace", "trace", new {area="VirtualMakeover"})" type="file" name="ImageFile" class="hide" />
</div>

<ul class="user-photos" style="margin-bottom: 10px;">
    @Html.Partial("PartialPhoto", (string)ViewBag.UserPhotoFileName)
</ul>

Select a picture
<ul class="model-photos">
    @foreach(var m in Model)
    {
        @Html.Partial("PartialModelPhoto", m)
    }
</ul>*@

<div class="col-sm-5" style="padding-bottom: 15px;">
    <div class="upload-instruction">
        <h4 class="headline clearfix"><span>Use Your Photo</span></h4>

        <span class="label label-warning" style="font-size: 0.9em;">                
            Please follow the below instructions to obtain best results.
        </span>

        <ul>
            <li>Use a high resolution, straight facing photo.</li>
            <li>Image should be in JPG or PNG format.</li>
            <li>Face without make-up and hair away from face.</li>
            <li>Picture size should be atleast 300 x 300 pixels.</li>
            <li>Bright and evenly lit photo</li>
            <li>Avoid web cam or low quality pictures</li>
        </ul>
        
        <div class="row">
            <div class="col-sm-4">
                <div class="good-photo">
                    <h4>Good photo</h4>
                    <img src="@Url.Content("~/assets/images/good-photo-01.jpg")" alt="good photo"/>
                </div>
            </div>

            <div class="col-sm-8">
                <div class="bad-photos">
                    <h4>Bad photos</h4>
                    <img src="@Url.Content("~/assets/images/bad-photo-01.jpg")" alt="bad photo"/>
                    <img src="@Url.Content("~/assets/images/bad-photo-02.jpg")" alt="bad photo"/>
                </div>
            </div>
        </div>
        
        <p class="lead clearfix" style="margin-bottom: 5px; margin-top: 10px;">
            Upload from your computer 
            <input type="button" class="btn btn-sm btn-primary" value="Upload" id="uploadPhoto" style="margin-left: 15px;" />
            <input id="ImageFile" data-name="userPhoto" type="file" name="ImageFile" class="hide" value="Upload"
                   data-url="@Url.Action("new-trace", "trace", new {area="VirtualMakeover"})" />
        </p>
        
        <div class="busy clearfix">
            <img src="@Url.Content("~/assets/images/ajax-loader.gif")" alt="loading..." width="110" height="10" 
                 style="margin-bottom: 10px;"/>
        </div>

    </div>
</div>

<div class="col-sm-1">
    <h4 style="padding-left: 15px;">Or</h4>
</div>

<div class="col-sm-5">
    <div class="traced-photos">

        <h4 class="headline clearfix"><span>Your uploaded photo</span></h4>

        <div class="row user-photos">
            @if(!string.IsNullOrEmpty(ViewBag.UserPhotoFileName))
            {
                @Html.Partial("PartialPhoto", (string) ViewBag.UserPhotoFileName)
            }else
            {
                <span style="display: block; margin-left: 30px;">None</span>
            }
        </div>

        <h4 class="headline clearfix"><span>Select a Model</span></h4>
        
        @*<ul class="model-photos">
            @foreach(var m in Model)
            {
                @Html.Partial("PartialModelPhoto", m)
            }
        </ul>*@
        <div class="row model-photos">        
            @foreach(var m in Model)
            {
                @Html.Partial("PartialModelPhoto", m)
            }
        </div>

    </div>
</div>